SELECT * FROM Users WHERE Age >= 18;  -- selects all adult users, or in english, "find all users who are 18 and older"
quit; -- quits the terminal

-- MySQL Workbench tutorial = section 2.10
-- access MySQL command terminal through start search, it will be called "MySQL 8.0 Command Line Client"
